package com.example.fptacademysystem.repository;import com.example.fptacademysystem.model.Semester;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import java.util.List;import org.springframework.data.repository.query.Param;@Repositorypublic interface SemesterRepository extends JpaRepository<Semester, Integer> {    // Find All Semester    @Query(value = "select * from Semester where semid!=1 and removeat = 'No'", nativeQuery = true)    @Override    List<Semester> findAll();    // Find All Semester    @Query(value = "SELECT s FROM Semester s WHERE s.semid!=1 AND s.removeat = 'No' AND s.semid = :semid")    Semester findOneSemesterById(@Param("semid")int semid);}