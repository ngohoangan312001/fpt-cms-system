// const rollnumEl = document.querySelector('#lecrollnum');// const passwordEl = document.querySelector('#lecpass');//// const form = document.querySelector('#leclogin');////// const required = value => value === '' ? false : true;//// const showError = (input, message) => {//     const formField = input.parentElement;////     input.classList.remove('is-valid');//     input.classList.add('is-invalid');////     input.focus();////     formField.classList.remove('success');//     formField.classList.add('error');////     // show message error with wrong input//     const error = formField.querySelector('small');//     error.textContent = message;// };//// const showSuccess = (input) => {////     const formField = input.parentElement;////     formField.classList.remove('error');//     formField.classList.add('success');////     // hide message error//     const error = formField.querySelector('small');//     error.textContent = ''; // gán rỗng để ẩn message lỗi// };//// // Validate Rollnum// const checkRollnum = () => {//     let valid = false;//     const rollnum = rollnumEl.value.trim();////     if (!required(rollnum)) {//         showError(rollnumEl, 'Rollnum cannot be blank !');//     } else {//         showSuccess(rollnumEl);//         valid = true;//     }//     return valid;// }//// // Validate Password// const checkPassword = () => {//     let valid = false;//     const pass = passwordEl.value.trim();////     if (!required(pass)){//         showError(passwordEl, 'Password cannot be blank !');//     } else {//         showSuccess(passwordEl);//         valid = true;//     }//     return valid;// };////// // Prevent the form submit// form.addEventListener('submit', function (e) {//     e.preventDefault();////     // validate form//     let isRollnumValid = checkRollnum(),//         isPasswordValid = checkPassword();////     let isFormValid = isRollnumValid  && isPasswordValid;////     // submit to the server if the form is valid//     if (isFormValid) {////     }// });//// form.addEventListener('input', function(e){//     switch (e.target.id) {//         case 'rollnum': checkRollnum();//             break;//         case 'pass': checkPassword();//             break;//     }// });